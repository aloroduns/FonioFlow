"use client";import {BarChart,Bar,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis} from "recharts";import {fonioData} from "@/lib/data";import {currencyFormat} from "@/lib/format";import {ScreenTitle} from "./shared";
export function PriceScreen(){return <div className="page"><ScreenTitle code="H4" title="Price Competitiveness" text="Compare like-for-like whole grains while keeping retail, producer and trade prices separate."/><div className="two-col"><div className="chart-card"><ResponsiveContainer width="100%" height={330}><BarChart data={fonioData.prices}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="Grain"/><YAxis/><Tooltip/><Bar dataKey="Average USD/kg" fill="#7C5D8E" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer></div><div className="price-stack">{fonioData.prices.map(price=><article key={price.Grain}><span>{price.Grain}</span><strong>{currencyFormat.format(price['Average USD/kg'])}/kg</strong><small>{price['Comparable products']} observations • {price['Evidence status']}</small></article>)}</div></div><div className="warning">The retail comparison is a convenience sample. Current price-per-kg remains incomplete where exact product URLs or package sizes are unavailable.</div></div>}
