"use client";import {useState} from "react";import {Search} from "lucide-react";import {fonioData} from "@/lib/data";import {EvidenceStatus,ScreenTitle} from "./shared";
export function SellersScreen(){const [query,setQuery]=useState("");const sellers=fonioData.sellers.filter(s=>`${s.seller_name} ${s.market} ${s.product_form} ${s.origin}`.toLowerCase().includes(query.toLowerCase()));return <div className="page"><ScreenTitle code="H5" title="Find Fonio" text="Search verified seller records by market, origin, seller or product form."/><div className="searchbox"><Search/><input aria-label="Search sellers" placeholder="Try France, flour, Burkina Faso…" value={query} onChange={e=>setQuery(e.target.value)}/><span>{sellers.length} results</span></div><div className="seller-grid">{sellers.map(s=><article key={s.seller_id}><div><EvidenceStatus>{s.verification_status}</EvidenceStatus><h3>{s.seller_name}</h3><p>{s.market} • {s.channel}</p></div><strong>{s.product_form}</strong><p>Origin: {s.origin}</p><small>{s.availability_status} • checked {s.verified_date}</small><a href={s.source_url} target="_blank" rel="noreferrer">View source</a></article>)}</div></div>}
