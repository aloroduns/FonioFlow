import {MapPin} from "lucide-react";import {fonioData} from "@/lib/data";import {EvidenceStatus,Metric,ScreenTitle} from "./shared";
export function ProcessingScreen(){return <div className="page"><ScreenTitle code="H2" title="Processing Landscape" text="Review identified facilities, processing stages, reported capacity and unresolved evidence gaps."/><div className="metric-grid four"><Metric label="Initiatives" value={`${fonioData.processors.length}`}/><Metric label="Capacity observations" value={`${fonioData.capacities.length}`}/><Metric label="Current capacity records" value="2"/><Metric label="Decision" value="Not concluded"/></div><div className="card-list">{fonioData.processors.map(p=><article key={p.record_id}><div><EvidenceStatus>{p.evidence_strength} evidence</EvidenceStatus><h3>{p.name}</h3><p><MapPin/> {p.location} • {p.country_coverage}</p></div><p>{p.processing_stages}</p><strong>{p.capacity_evidence}</strong><small>Missing: {p.missing_indicators}</small></article>)}</div></div>}
