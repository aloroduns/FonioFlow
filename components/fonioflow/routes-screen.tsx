"use client";
import {useApiDataset} from "@/hooks/use-api-dataset";
import {fonioData} from "@/lib/data";
import type {RouteRecord} from "@/lib/types";
import {DataStatus,ScreenTitle} from "./shared";
export function RoutesScreen(){const {data,meta,loading}=useApiDataset<RouteRecord>("/api/routes",fonioData.routes,"UN Comtrade route observations and verified road-route evidence");return <div className="page"><ScreenTitle code="H3" title="Distribution Route Explorer" text="Compare candidate inland routes to gateway ports and international markets."/><DataStatus meta={meta} loading={loading}/><div className="route-layout"><div className="route-map">{data.map((route,index)=><div key={route.route_id} className="route-line" style={{top:`${8+index*8}%`,width:`${38+index*3}%`}}><span>{route.origin_location}</span><i/><b>{route.destination_port}</b></div>)}</div><div className="route-table">{fonioData.roadRoutes.map(route=><article key={route['Route ID']}><strong>{route.Origin} → {route.Destination}</strong><span>{route['Distance km']} km</span><span>{route['Driving time']}</span><small>{route['International borders']} border(s)</small></article>)}</div></div><div className="warning">Freight quotations, shipment losses, border delays and site-specific storage remain missing. Routes are planning evidence, not proof that fonio travelled on the lane.</div></div>}
