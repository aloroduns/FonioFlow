export const numberFormat=new Intl.NumberFormat("en-US",{maximumFractionDigits:1});
export const currencyFormat=new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:2});
