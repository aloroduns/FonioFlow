import assert from "node:assert/strict";import fs from "node:fs";import test from "node:test";import path from "node:path";
const read=name=>JSON.parse(fs.readFileSync(path.join("data/generated",name),"utf8"));
test("all six data contracts contain records",()=>{for(const file of fs.readdirSync("data/generated").filter(x=>x.endsWith(".json"))){const payload=read(file);for(const [key,rows] of Object.entries(payload).filter(([,value])=>Array.isArray(value)))assert.ok(rows.length>0,`${file}:${key} is empty`)}});
test("production years and values are numeric",()=>{for(const row of read("h1-production.json").production){assert.equal(typeof row.year,"number");assert.equal(typeof row.production_tonnes,"number")}});
test("published seller records retain evidence links",()=>{for(const seller of read("h5-sellers.json").sellers){assert.ok(seller.source_url?.startsWith("http"));assert.ok(seller.verification_status)}});
test("H6 survey export matches the submitted-file audit",()=>{const payload=read("h6-demand.json");assert.equal(payload.surveyAudit.submitted_records,150);assert.equal(payload.surveyAudit.exact_duplicate_rows,102);assert.equal(payload.surveyAudit.unique_timestamps,29);const awareness=payload.survey.filter(row=>row.Measure==="Awareness");assert.equal(awareness.reduce((sum,row)=>sum+row.Count,0),150);assert.equal(awareness.find(row=>row["Response option"]==="Yes").Count,37)});
